[pytest]
pythonpath = src .
testpaths = tests
addopts = -m "not slow and not nightly"
markers =
    slow: marks tests as slow (full game simulations; run with -m slow)
    nightly: marks tests for nightly CI only (multi-minute training runs; run with -m nightly)
