[pytest]
pythonpath = src .
testpaths = tests
addopts = -m "not slow and not nightly"
markers =
    slow: marks tests as slow (full game simulations; run with -m slow; keep each under ~30s wall)
    nightly: marks tests that routinely exceed ~30s wall time or long training runs (run with -m nightly)
